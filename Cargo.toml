[package]
name = "clipfix"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4", features = ["derive"] }
arboard = { version = "3.6", default-features = false }

[dev-dependencies]
assert_cmd = "2"
predicates = "3"
